spring:
  datasource:
    url: jdbc:tc:postgresql:15://localhost/chat_test_db
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: postgres
    password: postgres
    hikari:
      maximum-pool-size: 4
      minimum-idle: 0
      idle-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baselineOnMigrate: true
  mail:
    host: localhost
    port: 1025

app:
  frontend-base-url: http://localhost:5173
  mail-from: noreply@test.local
